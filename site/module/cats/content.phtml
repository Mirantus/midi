<ul class="ns">
	<?php
		if ($items) {
			foreach ($items as $item_params) {
				echo '<li id="item' . $item_params['id'] . '">
						<a href="/' . $site->module . '/cat/' . $item_params['id'] . '">' . $item_params['title'] . '</a> ';
				echo '</li>';
			}
		} else echo '<li>Данных нет</li>';
	?>
</ul>
<?=$pager;?>